<h1><%= @board.title %></h1>

<ol>
  <% @board.tunes.each do |tune| %>
    <li><%= render 'tunes/songs', { tune: tune } %></li>
  <% end %>
</ol>

<hr />
<%= link_to 'Edit Board', edit_board_path(@board) %>
<br />
<%= link_to 'Delete Board', board_path(@board), method: :delete %>
<br />
<%= link_to "Back to Boards", boards_path %>